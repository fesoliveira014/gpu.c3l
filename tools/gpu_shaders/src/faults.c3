module gpu_shaders;

faultdef
    PARSE_ERROR,
    LAYOUT_ERROR,
    DUPLICATE_SYMBOL,
    UNKNOWN_TYPE,
    IO_ERROR,
    USAGE_ERROR,
    DRIFT_DETECTED,
    SHADER_STAGE_ERROR,
    SHADER_COMPILE_ERROR,
    TOOL_NOT_FOUND;
