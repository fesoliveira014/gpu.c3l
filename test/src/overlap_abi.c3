// Generated by gen_shader_abi from the 'overlap' ABI schema - do not edit.
module gpu_bench;

import gpu;

const uint OVERLAP_WORKGROUP = 64;

struct OverlapComputeRoot {
    GpuAddress data_gpu;
    uint       elem_count;
    uint       iters;
}
$assert OverlapComputeRoot::size == 16;
$assert $reflect(OverlapComputeRoot.data_gpu).offset == 0;
$assert $reflect(OverlapComputeRoot.elem_count).offset == 8;
$assert $reflect(OverlapComputeRoot.iters).offset == 12;

struct OverlapFragRoot {
    uint iters;
    uint _pad0;
}
$assert OverlapFragRoot::size == 8;
$assert $reflect(OverlapFragRoot.iters).offset == 0;
$assert $reflect(OverlapFragRoot._pad0).offset == 4;
